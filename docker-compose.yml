services:
  server:
    build: ./backend
    container_name: balto-server-prod
    ports:
      - "8765:8765"
    volumes:
      # Mantém o banco de dados seguro
      - balto-dados:/backend/app/dados
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  balto-dados: